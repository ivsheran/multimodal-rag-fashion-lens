# Ollama
ollama:
  base_url: "http://localhost:11434"
  vision_model: "llava"
  embedding_model: "nomic-embed-text"
  llm_model: "mistral:7b"

# Storage
storage:
  chroma_path: "./chroma_db"
  collection_name: "fashion_lens_collection"

# Data
dataset:
  name: "ashraq/fashion-product-images-small"

# Retrieval
search:
  top_k: 10

# UI
gradio:
  title: "Fashion Lens"
  share: false
